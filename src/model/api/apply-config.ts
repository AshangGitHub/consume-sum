const namespace = '/v1/auth-client';

/**
 * @description: apply
 */
const apply = {
  list: `${namespace}/list`, //login
  save: `${namespace}`, //login
  edit: `${namespace}`, //login
  detail: `${namespace}`, //login
};

export { apply };
